name: Build

on:
  push:
  pull_request:
    branches:
      - main

jobs:
  build-front:
    name: Build — Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/workflows/actions/setup-bun

      - name: Build
        uses: ./.github/workflows/actions/build-front

  build-back:
    name: Build — Backend
    runs-on: ubuntu-latest
    needs: build-front
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/workflows/actions/setup-rust

      - name: Build
        uses: ./.github/workflows/actions/build-back

  build-android:
    name: Build — Android
    runs-on: ubuntu-latest
    needs: build-front
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/workflows/actions/setup-bun

      - name: Setup Rust
        uses: ./.github/workflows/actions/setup-rust

      - name: Build
        uses: ./.github/workflows/actions/build-android
        with:
          keystore-base64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          keystore-password: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          key-alias: ${{ secrets.ANDROID_KEY_ALIAS }}
          key-password: ${{ secrets.ANDROID_KEY_PASSWORD }}
